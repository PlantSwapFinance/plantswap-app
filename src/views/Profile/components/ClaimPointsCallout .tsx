// No-op stub preserved so `src/views/Profile/TaskCenter.tsx` can keep mounting
// this component without changing its layout. Delete this file (and the
// corresponding import/JSX in TaskCenter) when a non-stub claim flow is added.
const ClaimPointsCallout = () => null

export default ClaimPointsCallout

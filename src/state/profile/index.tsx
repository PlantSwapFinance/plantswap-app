// Backwards-compatible re-export of the profile action surface.
export {
  profileFetchStart,
  profileFetchSucceeded,
  profileFetchFailed,
  profileClear,
  addPoints,
  fetchProfile,
  useProfileStore,
} from './store'